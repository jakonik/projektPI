<!DOCTYPE html>
 
html(lang="pl")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Ankiety
        link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Montserrat:300,400,500,700")
        link(rel="stylesheet" href="/css/main.css")
    body
        .home.ankiety
            header.app-header
                
                nav.app-nav
                    ul.app-nav__list
                        li.app-nav__list-item
                            a.app-nav__link.app-nav__link--is-active(href="#") home
                        li.app-nav__list-item
                            a.app-nav__link(href="/view") czy zmodyfikowano
                        li.app-nav__list-item
                            a.app-nav__link(href="/ifPass") czy wypełniono
            .wrapper
                section.slogan
                    h1.slogan__header
                        | Be a hero. Wypełnij ankietę.
                    p.slogan__text
                        | Jest anonimowa, pomoże w ocenie.
                    .actions
                        button.btn.btn--primary(@click="formLightbox = true") Wypełnij ankietę
                   
                        
                
            .lightbox(v-show="formLightbox == true" style="display: none;")
                .lightbox__content
                    h2.lightbox__header Wypełnij ankietę
                    form.lightbox__form(action="/applications" method="POST")
                        .lightbox__form-control
                            label.lightbox__label twoje haslo
                            input.lightbox__input(placeholder="zapamietaj je"  name="appHaslo")
                        .lightbox__form-control
                            label.lightbox__label twoje imie
                            input.lightbox__input(placeholder="imie"  name="usName")
                        .lightbox__form-control
                            label.lightbox__label twoje nazwisko
                            input.lightbox__input(placeholder="nazwisko"  name="usSurname")
                        .lightbox__form-control
                            label.lightbox__label twoj mail
                            input.lightbox__input(placeholder="mail"  name="usMail")
                        .lightbox__form-control
                            label.lightbox__label Twoja opinia słowna?
                            input.lightbox__input(placeholder="Było spoko"  name="appName")
                        .lightbox__form-control
                            label.lightbox__label Jaka jest twoja ocena?
                            input.lightbox__input(placeholder="od 1 do 10" name="appMark")
                        .lightbox__form-control
                            label.lightbox__label tutaj mozesz sie rozpisać
                            textarea.lightbox__input(placeholder="example@xd.com" name="appMessage")
                        .lightbox__actions
                            button.btn.btn--primary Wyślij
            if (formMessage.length)
                .lightbox(v-show="showFeedback")
                    .lightbox__content
                        h2.lightbox__header #{ formMessage }
                        .lightbox__actions.lightbox__actions--centered
                            button.btn.btn--primary(@click="closeFeedbackLightbox") Close


                            




        script(src="https://cdn.jsdelivr.net/npm/vue" )
        script(src="/js/app.js")